@model IEnumerable<Hammer.Models.Department>

@{
    ViewBag.Title = "Departments";
}

<h2>Departments</h2>

<p>
    @Html.ActionLink("Add New Department", "Create")
</p>
@using (Html.BeginForm())
{
    <p>
        Find department: @Html.TextBox("SearchString")
        <input type="submit" value="Search" />
    </p>
}
<table class="table">
    <tr>
        <th>
            @Html.ActionLink("Department Name", "Index", new { sortOrder = ViewBag.NameSortParm })
        </th>
        <th>
            @Html.ActionLink("Department Location", "Index", new { sortOrder = ViewBag.LocationSortParm })
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.departmentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.departmentLocation)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.departmentId }) |
                @Html.ActionLink("Details", "Details", new { id = item.departmentId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.departmentId })
            </td>
        </tr>
    }

</table>
